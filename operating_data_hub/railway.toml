[build]
builder = "DOCKERFILE"
dockerfilePath = "operating_data_hub/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

